# Core
websockets>=11.0.3
PyYAML>=6.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
psutil>=5.9.0

# Hardware (Pi)
RPi.GPIO>=0.7.1
smbus2>=0.4.0
rpi-ws281x>=5.0.0
adafruit-circuitpython-neopixel>=6.4.1
adafruit-circuitpython-dht>=3.4.0
adafruit-circuitpython-hcsr04>=0.4.0
adafruit-circuitpython-tsl2561>=3.2.0

# Camera / vision
# Primary backend: GStreamer via libcamerasrc — install with scripts/setup_pi_camera.sh
# picamera2 is a legacy fallback: sudo apt install python3-picamera2
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
Pillow>=10.0.0

# ROS2 — installed with ROS2, not via pip
# sudo apt install ros-humble-rclpy ros-humble-sensor-msgs ros-humble-std-msgs

# Sensors
w1thermsensor>=2.0.0
adafruit-circuitpython-mpu6050>=1.0.0
adafruit-circuitpython-bno055>=3.2.0

# Serial
pyserial>=3.5

# Data processing
numpy>=1.24.0
scipy>=1.11.0

# Computer Vision (basic)
imutils>=0.5.4
# face-recognition and dlib compile from source on aarch64 (very slow).
# Install separately only if needed: pip install -r requirements-optional.txt

# ML inference — Jetson only, install separately with CUDA-compatible wheels
# See docs/JETSON_PYTORCH.md for install instructions
# tensorflow>=2.13.0
# torch>=2.0.0
# torchvision>=0.15.0

# Logging
structlog>=23.1.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.7.0
flake8>=6.0.0
mypy>=1.5.0jinja2>=3.1.0
jinja2>=3.1.0
