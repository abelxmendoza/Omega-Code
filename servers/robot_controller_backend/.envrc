# Use the project's existing Python venv at ./venv
VENV="./venv"

if [ -d "$VENV" ]; then
  echo "direnv: using $VENV"
  PATH_add "$VENV/bin"
  export VIRTUAL_ENV="$(pwd)/$VENV"
else
  echo "direnv: venv not found at $VENV (create it with: python3 -m venv venv)"
fi

# use dotenv .env
