# Automatically activate Python virtual environment
# This uses direnv's built-in layout function which:
# 1. Creates venv if it doesn't exist
# 2. Activates it automatically when entering the directory
# 3. Deactivates when leaving

layout python3

# Load environment variables from .env file if it exists
if [ -f .env ]; then
  dotenv .env
fi
