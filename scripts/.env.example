# File: /Omega-Code/scripts/.env.example
# Summary:
#   Example environment for Bluetooth PAN helper scripts (macOS host + Raspberry Pi).
#   Safe to commit. Replace these placeholders in your private `.env`.
#
#   Notes:
#   - PHONE_MAC accepts either hyphens or colons (AA-BB-CC-... or AA:BB:CC:...).
#   - If you use Tailscale, set USE_TAILSCALE=1 and fill TAILSCALE_IP_PI or TS_HOST.
#   - PI_HOST is used by macOS script to SSH into the Pi.

# ==== Required ====

# iPhone (or phone) Bluetooth MAC address (placeholder; change in your real .env)
PHONE_MAC=aa-bb-cc-11-22-33      # e.g., from `blueutil --paired` (macOS) or `bluetoothctl devices` (Pi)

# How to reach the Pi from your Mac (hostname, TS name, or user@ip)
PI_HOST=robot@robot-tailscale     # examples: robot@100.64.0.42  |  robot@robot-pi.local

# Linux Bluetooth adapter name on the Pi (usually hci0)
HCI_DEVICE=hci0

# RFCOMM channel used by your phone’s PAN (commonly 1)
RFCOMM_CHANNEL=1


# ==== Optional (uncomment & set as needed) ====

# Prefer Tailscale when contacting the Pi from macOS (1=yes, 0=no)
# USE_TAILSCALE=1

# If using Tailscale, you can set the Pi’s TS IP/host explicitly:
# TAILSCALE_IP_PI=100.64.0.42
# TS_HOST=robot@100.64.0.42

# If you also use LAN at home, keeping a placeholder here is handy:
# PI_IP=192.168.50.42

# macOS: path to blueutil if not on PATH
# BLUEUTIL=/opt/homebrew/bin/blueutil

# Retry knobs for flaky cafés/hotspots
# BT_CONNECT_ATTEMPTS=3
# BT_CONNECT_SLEEP=3

# Verbose logging (1=on, 0=off)
# VERBOSE=1

